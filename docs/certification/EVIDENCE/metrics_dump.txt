# Métricas Prometheus – captura pendente (ambiente local sem stack)
# Instruções quando Prometheus/metrics endpoint estiver disponível:
#   curl -s $METRICS_URL | grep 'webhooks_\|hmac_\|billing_'
#   head -n 20 > docs/certification/EVIDENCE/metrics_dump.txt
# Esperado: contadores `webhooks_delivery_total{event="fee.applied",status="delivered"}`,
# histograma `webhooks_delivery_seconds_bucket`, `webhooks_failed_total{reason="missing_endpoint"}`,
# `webhooks_dead_letter_total{event="fee.applied"}`,
# além de métricas legadas (`billing_events_total`, `fees_applied_total`).
